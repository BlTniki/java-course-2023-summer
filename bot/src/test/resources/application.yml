spring:
  application:
    name: bot
  jackson:
    time-zone: UTC

app:
  telegram-token: "dawdawdaw"

kafka:
  consumer:
    topic:
      name: scrapper

client:
  scrapper:
    baseUrl: "none"

logging:
  config: classpath:log4j2-plain.xml
